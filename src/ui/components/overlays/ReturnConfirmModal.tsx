import ConfirmModal from "@/ui/components/overlays/ConfirmModal";

export default function ReturnConfirmModal() {
    return <ConfirmModal promptText={"Are you sure you want to return this product?"} />;
}
